@rendermode InteractiveServer
@inject NavigationManager NavigationManager

<button class="btn btn-primary" @onclick=AddGame>Add Game</button>

@code {
    void AddGame()
    {
        NavigationManager.NavigateTo("/game-form");
    }
}
